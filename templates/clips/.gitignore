build
.output/

.generated/

# Stray SQLite files at the template root (real DB lives under data/)
/app.db
/app.db-shm
/app.db-wal
/local.db

.vercel/

# Copied from @mediapipe/tasks-vision at build/dev start by vite.config.ts.
# The segmentation model alongside it (public/mediapipe/*.tflite) is vendored.
public/mediapipe/wasm/

# Vendored Remotion Editor Starter source (paid license, source is NOT
# redistributable — must never be committed while this repo is public).
# See docs/full-editor-integration-plan.md and the video-projects skill.
app/video-editor/
